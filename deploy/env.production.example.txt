# Copy this file to .env (project root) and adjust values

# Database (SQLite as per prisma/schema.prisma)
DATABASE_URL="file:./prisma/dev.db"

# NextAuth
NEXTAUTH_URL="https://time.yourdomain.com"  # replace with your domain
NEXTAUTH_SECRET="<generate-a-random-guid>"

# Optional email SMTP (leave empty if unused)
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT="587"
# SMTP_USER=""
# SMTP_PASS=""

# File Uploads (if used by your app)
# UPLOAD_DIR="./uploads"
# MAX_FILE_SIZE="10485760"


